import { Container } from "@/components/atom";
import { Seals } from "@/components/molecule";
import React from "react";

type Props = {};

const SealsTeam = (props: Props) => {
  return (
    <Container className="mb-[11.3rem]">
      <Seals />
    </Container>
  );
};

export default SealsTeam;
